[package]
name = "alkaidsd"
version = "1.0.0"
edition = "2021"
description = "Split Delivery Vehicle Routing Problem (SDVRP) solver"
authors = ["HellOwhatAs"]
license = "MIT"

[profile.release]
lto = true
codegen-units = 1

[dependencies]
itertools = "0.14.0"
